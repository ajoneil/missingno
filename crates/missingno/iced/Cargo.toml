[package]
name = "missingno-iced"
version.workspace = true
edition = "2024"

[dependencies]
missingno-core = { path = "../core" }
iced = { version = "0.14.0", features = ["advanced", "wgpu"] }
rgb = { workspace = true }
bytemuck = { version = "1.25", features = ["derive"] }
