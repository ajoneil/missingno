[package]
name = "missingno-core"
version.workspace = true
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
rgb = { workspace = true }
sha2 = { version = "0.11" }

[dev-dependencies]
ron = "0.12"
