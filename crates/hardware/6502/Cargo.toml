[package]
name = "missingno-6502"
version.workspace = true
edition = "2024"

[dependencies]
missingno-core = { path = "../../missingno/core" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
