[package]
name = "missingno-curator"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
iced = { version = "0.14.0", features = ["smol", "image"] }
jiff = "0.2"
missingno-core = { path = "../missingno-core" }
missingno-gamedb = { path = "../../missingno-gamedb/crates/gamedb" }
missingno-session = { path = "../missingno-session", features = ["tools"] }
serde_json = "1"
sha1 = "0.11"
smol = "2"
ureq = "3"
