[package]
name = "missingno-sms"
version = "0.1.0"
edition = "2024"

[dependencies]
missingno-core = { path = "../missingno-core" }
rgb = { workspace = true }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
