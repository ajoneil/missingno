[package]
name = "missingno-sms"
version.workspace = true
edition = "2024"

[dependencies]
missingno-core = { path = "../missingno-core" }
rgb = { workspace = true }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
